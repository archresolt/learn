package mychain;

public class CarChainNode {
Node previous ;//上一个节点
Node next;//下一个节点
Node element;//数据元素
public CarChainNode(Node previous,Node next, Node element ){
	super();
	 this.next = next;
	 this.element = element;
}
public CarChainNode(){
	super();
	this.element = element;
}
}
